(******************************************************************************

  %name:          nwerror.h %
  %version:       9 %
  %date_modified: Fri May  2 14:38:42 1997 %
  $Copyright:

  Copyright (c) 1989-1995 Novell, Inc.  All Rights Reserved.                      

  THIS WORK IS  SUBJECT  TO  U.S.  AND  INTERNATIONAL  COPYRIGHT  LAWS  AND
  TREATIES.   NO  PART  OF  THIS  WORK MAY BE  USED,  PRACTICED,  PERFORMED
  COPIED, DISTRIBUTED, REVISED, MODIFIED, TRANSLATED,  ABRIDGED, CONDENSED,
  EXPANDED,  COLLECTED,  COMPILED,  LINKED,  RECAST, TRANSFORMED OR ADAPTED
  WITHOUT THE PRIOR WRITTEN CONSENT OF NOVELL, INC. ANY USE OR EXPLOITATION
  OF THIS WORK WITHOUT AUTHORIZATION COULD SUBJECT THE PERPETRATOR TO
  CRIMINAL AND CIVIL LIABILITY.$

 *****************************************************************************)


{$IFNDEF  NWERROR_H }
    {$DEFINE NWERROR_H}
    (* Don't Do this file if NWErrno.h is included. *)
    {$IFNDEF  _NWERRNO_H_ }
        (**** SDK success condition *)
        {$IFNDEF SUCCESS}
        {$DEFINE SUCCESS}
        Const SUCCESS = 0;
        {$ENDIF}
        (**** Network errors ****)
        (* Decimal values at end of line are 32768 lower than actual *)
        {$DEFINE SHELL_ERROR}
        Const SHELL_ERROR = $8800;
        {$DEFINE VLM_ERROR}
        Const VLM_ERROR = $8800;
        {$DEFINE ALREADY_ATTACHED}
        Const ALREADY_ATTACHED = $8800;
        {$DEFINE INVALID_CONNECTION}
        Const INVALID_CONNECTION = $8801;
        {$DEFINE DRIVE_IN_USE}
        Const DRIVE_IN_USE = $8802;
        {$DEFINE CANT_ADD_CDS}
        Const CANT_ADD_CDS = $8803;
        {$DEFINE DRIVE_CANNOT_MAP}
        Const DRIVE_CANNOT_MAP = $8803;
        {$DEFINE BAD_DRIVE_BASE}
        Const BAD_DRIVE_BASE = $8804;
        {$DEFINE NET_READ_ERROR}
        Const NET_READ_ERROR = $8805;
        {$DEFINE NET_RECV_ERROR}
        Const NET_RECV_ERROR = $8805;
        {$DEFINE UNKNOWN_NET_ERROR}
        Const UNKNOWN_NET_ERROR = $8806;
        {$DEFINE SERVER_INVALID_SLOT}
        Const SERVER_INVALID_SLOT = $8807;
        {$DEFINE BAD_SERVER_SLOT}
        Const BAD_SERVER_SLOT = $8807;
        {$DEFINE NO_SERVER_SLOTS}
        Const NO_SERVER_SLOTS = $8808;
        {$DEFINE NET_WRITE_ERROR}
        Const NET_WRITE_ERROR = $8809;
        {$DEFINE CONNECTION_IN_ERROR_STATE}
        Const CONNECTION_IN_ERROR_STATE = $8809;
        {$DEFINE NET_SEND_ERROR}
        Const NET_SEND_ERROR = $8809;
        {$DEFINE SERVER_NO_ROUTE}
        Const SERVER_NO_ROUTE = $880A;
        {$DEFINE BAD_LOCAL_TARGET}
        Const BAD_LOCAL_TARGET = $880B;
        {$DEFINE TOO_MANY_REQ_FRAGS}
        Const TOO_MANY_REQ_FRAGS = $880C;
        {$DEFINE CONNECT_LIST_OVERFLOW}
        Const CONNECT_LIST_OVERFLOW = $880D;
        {$DEFINE BUFFER_OVERFLOW}
        Const BUFFER_OVERFLOW = $880E;
        {$DEFINE MORE_DATA_ERROR}
        Const MORE_DATA_ERROR = $880E;
        {$DEFINE NO_CONN_TO_SERVER}
        Const NO_CONN_TO_SERVER = $880F;
        {$DEFINE NO_CONNECTION_TO_SERVER}
        Const NO_CONNECTION_TO_SERVER = $880F;
        {$DEFINE NO_ROUTER_FOUND}
        Const NO_ROUTER_FOUND = $8810;
        {$DEFINE BAD_FUNC_ERROR}
        Const BAD_FUNC_ERROR = $8811;
        {$DEFINE INVALID_SHELL_CALL}
        Const INVALID_SHELL_CALL = $8811;
        {$DEFINE SCAN_COMPLETE}
        Const SCAN_COMPLETE = $8812;
        {$DEFINE LIP_RESIZE_ERROR}
        Const LIP_RESIZE_ERROR = $8812;
        {$DEFINE UNSUPPORTED_NAME_FORMAT_TYPE}
        Const UNSUPPORTED_NAME_FORMAT_TYPE = $8813;
        {$DEFINE INVALID_DIR_HANDLE}
        Const INVALID_DIR_HANDLE = $8813;
        {$DEFINE HANDLE_ALREADY_LICENSED}
        Const HANDLE_ALREADY_LICENSED = $8814;
        {$DEFINE OUT_OF_CLIENT_MEMORY}
        Const OUT_OF_CLIENT_MEMORY = $8814;
        {$DEFINE HANDLE_ALREADY_UNLICENSED}
        Const HANDLE_ALREADY_UNLICENSED = $8815;
        {$DEFINE PATH_NOT_OURS}
        Const PATH_NOT_OURS = $8815;
        {$DEFINE INVALID_NCP_PACKET_LENGTH}
        Const INVALID_NCP_PACKET_LENGTH = $8816;
        {$DEFINE PATH_IS_PRINT_DEVICE}
        Const PATH_IS_PRINT_DEVICE = $8816;
        {$DEFINE SETTING_UP_TIMEOUT}
        Const SETTING_UP_TIMEOUT = $8817;
        {$DEFINE PATH_IS_EXCLUDED_DEVICE}
        Const PATH_IS_EXCLUDED_DEVICE = $8817;
        {$DEFINE SETTING_SIGNALS}
        Const SETTING_SIGNALS = $8818;
        {$DEFINE PATH_IS_INVALID}
        Const PATH_IS_INVALID = $8818;
        {$DEFINE SERVER_CONNECTION_LOST}
        Const SERVER_CONNECTION_LOST = $8819;
        {$DEFINE NOT_SAME_DEVICE}
        Const NOT_SAME_DEVICE = $8819;
        {$DEFINE OUT_OF_HEAP_SPACE}
        Const OUT_OF_HEAP_SPACE = $881A;
        {$DEFINE INVALID_SERVICE_REQUEST}
        Const INVALID_SERVICE_REQUEST = $881B;
        {$DEFINE INVALID_SEARCH_HANDLE}
        Const INVALID_SEARCH_HANDLE = $881B;
        {$DEFINE INVALID_TASK_NUMBER}
        Const INVALID_TASK_NUMBER = $881C;
        {$DEFINE INVALID_DEVICE_HANDLE}
        Const INVALID_DEVICE_HANDLE = $881C;
        {$DEFINE INVALID_MESSAGE_LENGTH}
        Const INVALID_MESSAGE_LENGTH = $881D;
        {$DEFINE INVALID_SEM_HANDLE}
        Const INVALID_SEM_HANDLE = $881D;
        {$DEFINE EA_SCAN_DONE}
        Const EA_SCAN_DONE = $881E;
        {$DEFINE INVALID_CFG_HANDLE}
        Const INVALID_CFG_HANDLE = $881E;
        {$DEFINE BAD_CONNECTION_NUMBER}
        Const BAD_CONNECTION_NUMBER = $881F;
        {$DEFINE INVALID_MOD_HANDLE}
        Const INVALID_MOD_HANDLE = $881F;
        {$DEFINE ASYN_FIRST_PASS}
        Const ASYN_FIRST_PASS = $8820;
        {$DEFINE INVALID_DEVICE_INDEX}
        Const INVALID_DEVICE_INDEX = $8821;
        {$DEFINE INVALID_CONN_HANDLE}
        Const INVALID_CONN_HANDLE = $8822;
        {$DEFINE INVALID_QUEUE_ID}
        Const INVALID_QUEUE_ID = $8823;
        {$DEFINE INVALID_PDEVICE_HANDLE}
        Const INVALID_PDEVICE_HANDLE = $8824;
        {$DEFINE INVALID_JOB_HANDLE}
        Const INVALID_JOB_HANDLE = $8825;
        {$DEFINE INVALID_ELEMENT_ID}
        Const INVALID_ELEMENT_ID = $8826;
        {$DEFINE ALIAS_NOT_FOUND}
        Const ALIAS_NOT_FOUND = $8827;
        {$DEFINE RESOURCE_SUSPENDED}
        Const RESOURCE_SUSPENDED = $8828;
        {$DEFINE INVALID_QUEUE_SPECIFIED}
        Const INVALID_QUEUE_SPECIFIED = $8829;
        {$DEFINE DEVICE_ALREADY_OPEN}
        Const DEVICE_ALREADY_OPEN = $882A;
        {$DEFINE JOB_ALREADY_OPEN}
        Const JOB_ALREADY_OPEN = $882B;
        {$DEFINE QUEUE_NAME_ID_MISMATCH}
        Const QUEUE_NAME_ID_MISMATCH = $882C;
        {$DEFINE JOB_ALREADY_STARTED}
        Const JOB_ALREADY_STARTED = $882D;
        {$DEFINE SPECT_DAA_TYPE_NOT_SUPPORTED}
        Const SPECT_DAA_TYPE_NOT_SUPPORTED = $882E;
        {$DEFINE INVALID_ENVIR_HANDLE}
        Const INVALID_ENVIR_HANDLE = $882F;
        {$DEFINE NOT_SAME_CONNECTION}
        Const NOT_SAME_CONNECTION = $8830;
        {$DEFINE PRIMARY_CONNECTION_NOT_SET}
        Const PRIMARY_CONNECTION_NOT_SET = $8831;
        {$DEFINE NO_PRIMARY_SET}
        Const NO_PRIMARY_SET = $8831;
        {$DEFINE KEYWORD_NOT_FOUND}
        Const KEYWORD_NOT_FOUND = $8832;
        {$DEFINE PRINT_CAPTURE_NOT_IN_PROGRESS}
        Const PRINT_CAPTURE_NOT_IN_PROGRESS = $8832;
        {$DEFINE NO_CAPTURE_SET}
        Const NO_CAPTURE_SET = $8832;
        {$DEFINE NO_CAPTURE_IN_PROGRESS}
        Const NO_CAPTURE_IN_PROGRESS = $8832;
        {$DEFINE BAD_BUFFER_LENGTH}
        Const BAD_BUFFER_LENGTH = $8833;
        {$DEFINE INVALID_BUFFER_LENGTH}
        Const INVALID_BUFFER_LENGTH = $8833;
        {$DEFINE NO_USER_NAME}
        Const NO_USER_NAME = $8834;
        {$DEFINE NO_NETWARE_PRINT_SPOOLER}
        Const NO_NETWARE_PRINT_SPOOLER = $8835;
        {$DEFINE INVALID_PARAMETER}
        Const INVALID_PARAMETER = $8836;
        {$DEFINE CONFIG_FILE_OPEN_FAILED}
        Const CONFIG_FILE_OPEN_FAILED = $8837;
        {$DEFINE NO_CONFIG_FILE}
        Const NO_CONFIG_FILE = $8838;
        {$DEFINE CONFIG_FILE_READ_FAILED}
        Const CONFIG_FILE_READ_FAILED = $8839;
        {$DEFINE CONFIG_LINE_TOO_LONG}
        Const CONFIG_LINE_TOO_LONG = $883A;
        {$DEFINE CONFIG_LINES_IGNORED}
        Const CONFIG_LINES_IGNORED = $883B;
        {$DEFINE NOT_MY_RESOURCE}
        Const NOT_MY_RESOURCE = $883C;
        {$DEFINE DAEMON_INSTALLED}
        Const DAEMON_INSTALLED = $883D;
        {$DEFINE SPOOLER_INSTALLED}
        Const SPOOLER_INSTALLED = $883E;
        {$DEFINE CONN_TABLE_FULL}
        Const CONN_TABLE_FULL = $883F;
        {$DEFINE CONNECTION_TABLE_FULL}
        Const CONNECTION_TABLE_FULL = $883F;
        {$DEFINE CONFIG_SECTION_NOT_FOUND}
        Const CONFIG_SECTION_NOT_FOUND = $8840;
        {$DEFINE BAD_TRAN_TYPE}
        Const BAD_TRAN_TYPE = $8841;
        {$DEFINE INVALID_TRANSPORT_TYPE}
        Const INVALID_TRANSPORT_TYPE = $8841;
        {$DEFINE TDS_TAG_IN_USE}
        Const TDS_TAG_IN_USE = $8842;
        {$DEFINE TDS_OUT_OF_MEMORY}
        Const TDS_OUT_OF_MEMORY = $8843;
        {$DEFINE TDS_INVALID_TAG}
        Const TDS_INVALID_TAG = $8844;
        {$DEFINE TDS_WRITE_TRUNCATED}
        Const TDS_WRITE_TRUNCATED = $8845;
        {$DEFINE NO_CONNECTION_TO_DS}
        Const NO_CONNECTION_TO_DS = $8846;
        {$DEFINE NO_DIRECTORY_SERVICE_CONNECTION}
        Const NO_DIRECTORY_SERVICE_CONNECTION = $8846;
        {$DEFINE SERVICE_BUSY}
        Const SERVICE_BUSY = $8846;
        {$DEFINE NO_SERVER_ERROR}
        Const NO_SERVER_ERROR = $8847;
        {$DEFINE BAD_VLM_ERROR}
        Const BAD_VLM_ERROR = $8848;
        {$DEFINE NETWORK_DRIVE_IN_USE}
        Const NETWORK_DRIVE_IN_USE = $8849;
        {$DEFINE LOCAL_DRIVE_IN_USE}
        Const LOCAL_DRIVE_IN_USE = $884A;
        {$DEFINE NO_DRIVES_AVAILABLE}
        Const NO_DRIVES_AVAILABLE = $884B;
        {$DEFINE DEVICE_NOT_REDIRECTED}
        Const DEVICE_NOT_REDIRECTED = $884C;
        {$DEFINE NO_MORE_SFT_ENTRIES}
        Const NO_MORE_SFT_ENTRIES = $884D;
        {$DEFINE UNLOAD_ERROR}
        Const UNLOAD_ERROR = $884E;
        {$DEFINE IN_USE_ERROR}
        Const IN_USE_ERROR = $884F;
        {$DEFINE TOO_MANY_REP_FRAGS}
        Const TOO_MANY_REP_FRAGS = $8850;
        {$DEFINE TABLE_FULL}
        Const TABLE_FULL = $8851;
        {$DEFINE SOCKET_NOT_OPEN}
        Const SOCKET_NOT_OPEN = $8852;
        {$DEFINE MEM_MGR_ERROR}
        Const MEM_MGR_ERROR = $8853;
        {$DEFINE SFT3_ERROR}
        Const SFT3_ERROR = $8854;
        {$DEFINE PREFERRED_NOT_FOUND}
        Const PREFERRED_NOT_FOUND = $8855;
        {$DEFINE DEVICE_NOT_RECOGNIZED}
        Const DEVICE_NOT_RECOGNIZED = $8856;
        {$DEFINE BAD_NET_TYPE}
        Const BAD_NET_TYPE = $8857;
        {$DEFINE ERROR_OPENING_FILE}
        Const ERROR_OPENING_FILE = $8858;
        {$DEFINE NO_PREFERRED_SPECIFIED}
        Const NO_PREFERRED_SPECIFIED = $8859;
        {$DEFINE ERROR_OPENING_SOCKET}
        Const ERROR_OPENING_SOCKET = $885A;
        {$DEFINE REQUESTER_FAILURE}
        Const REQUESTER_FAILURE = $885A;
        {$DEFINE RESOURCE_ACCESS_DENIED}
        Const RESOURCE_ACCESS_DENIED = $885B;
        {$DEFINE SIGNATURE_LEVEL_CONFLICT}
        Const SIGNATURE_LEVEL_CONFLICT = $8861;
        {$DEFINE NO_LOCK_FOUND}
        Const NO_LOCK_FOUND = $8862;
        {$DEFINE LOCK_TABLE_FULL}
        Const LOCK_TABLE_FULL = $8863;
        {$DEFINE INVALID_MATCH_DATA}
        Const INVALID_MATCH_DATA = $8864;
        {$DEFINE MATCH_FAILED}
        Const MATCH_FAILED = $8865;
        {$DEFINE NO_MORE_ENTRIES}
        Const NO_MORE_ENTRIES = $8866;
        {$DEFINE INSUFFICIENT_RESOURCES}
        Const INSUFFICIENT_RESOURCES = $8867;
        {$DEFINE STRING_TRANSLATION}
        Const STRING_TRANSLATION = $8868;
        {$DEFINE STRING_TRANSLATION_NEEDED}
        Const STRING_TRANSLATION_NEEDED = $8868;
        {$DEFINE ACCESS_VIOLATION}
        Const ACCESS_VIOLATION = $8869;
        {$DEFINE NOT_AUTHENTICATED}
        Const NOT_AUTHENTICATED = $886A;
        {$DEFINE INVALID_LEVEL}
        Const INVALID_LEVEL = $886B;
        {$DEFINE RESOURCE_LOCK_ERROR}
        Const RESOURCE_LOCK_ERROR = $886C;
        {$DEFINE INVALID_NAME_FORMAT}
        Const INVALID_NAME_FORMAT = $886D;
        {$DEFINE OBJECT_EXISTS}
        Const OBJECT_EXISTS = $886E;
        {$DEFINE OBJECT_NOT_FOUND}
        Const OBJECT_NOT_FOUND = $886F;
        {$DEFINE UNSUPPORTED_TRAN_TYPE}
        Const UNSUPPORTED_TRAN_TYPE = $8870;
        {$DEFINE INVALID_STRING_TYPE}
        Const INVALID_STRING_TYPE = $8871;
        {$DEFINE INVALID_OWNER}
        Const INVALID_OWNER = $8872;
        {$DEFINE UNSUPPORTED_AUTHENTICATOR}
        Const UNSUPPORTED_AUTHENTICATOR = $8873;
        {$DEFINE IO_PENDING}
        Const IO_PENDING = $8874;
        {$DEFINE INVALID_DRIVE_NUM}
        Const INVALID_DRIVE_NUM = $8875;
        {$DEFINE SHELL_FAILURE}
        Const SHELL_FAILURE = $88FF;
        {$DEFINE VLM_FAILURE}
        Const VLM_FAILURE = $88FF;
        {$DEFINE SVC_ALREADY_REGISTERED}
        Const SVC_ALREADY_REGISTERED = $8880;
        {$DEFINE SVC_REGISTRY_FULL}
        Const SVC_REGISTRY_FULL = $8881;
        {$DEFINE SVC_NOT_REGISTERED}
        Const SVC_NOT_REGISTERED = $8882;
        {$DEFINE OUT_OF_RESOURCES}
        Const OUT_OF_RESOURCES = $8883;
        {$DEFINE RESOLVE_SVC_FAILED}
        Const RESOLVE_SVC_FAILED = $8884;
        {$DEFINE CONNECT_FAILED}
        Const CONNECT_FAILED = $8885;
        {$DEFINE PROTOCOL_NOT_BOUND}
        Const PROTOCOL_NOT_BOUND = $8886;
        {$DEFINE AUTHENTICATION_FAILED}
        Const AUTHENTICATION_FAILED = $8887;
        {$DEFINE INVALID_AUTHEN_HANDLE}
        Const INVALID_AUTHEN_HANDLE = $8888;
        {$DEFINE AUTHEN_HANDLE_ALREADY_EXISTS}
        Const AUTHEN_HANDLE_ALREADY_EXISTS = $8889;
        {$DEFINE DIFF_OBJECT_ALREADY_AUTHEN}
        Const DIFF_OBJECT_ALREADY_AUTHEN = $8890;
        {$DEFINE REQUEST_NOT_SERVICEABLE}
        Const REQUEST_NOT_SERVICEABLE = $8891;
        {$DEFINE AUTO_RECONNECT_SO_REBUILD}
        Const AUTO_RECONNECT_SO_REBUILD = $8892;
        {$DEFINE AUTO_RECONNECT_RETRY_REQUEST}
        Const AUTO_RECONNECT_RETRY_REQUEST = $8893;
        {$DEFINE ASYNC_REQUEST_IN_USE}
        Const ASYNC_REQUEST_IN_USE = $8894;
        {$DEFINE ASYNC_REQUEST_CANCELED}
        Const ASYNC_REQUEST_CANCELED = $8895;
        {$DEFINE SESS_SVC_ALREADY_REGISTERED}
        Const SESS_SVC_ALREADY_REGISTERED = $8896;
        {$DEFINE SESS_SVC_NOT_REGISTERED}
        Const SESS_SVC_NOT_REGISTERED = $8897;
        {$DEFINE PREVIOUSLY_AUTHENTICATED}
        Const PREVIOUSLY_AUTHENTICATED = $8899;
        {$DEFINE RESOLVE_SVC_PARTIAL}
        Const RESOLVE_SVC_PARTIAL = $889A;
        {$DEFINE NO_DEFAULT_SPECIFIED}
        Const NO_DEFAULT_SPECIFIED = $889B;
        {$DEFINE HOOK_REQUEST_NOT_HANDLED}
        Const HOOK_REQUEST_NOT_HANDLED = $889C;
        {$DEFINE HOOK_REQUEST_BUSY}
        Const HOOK_REQUEST_BUSY = $889D;
        {$DEFINE HOOK_REQUEST_QUEUED}
        Const HOOK_REQUEST_QUEUED = $889D;
        {$DEFINE AUTO_RECONNECT_SO_IGNORE}
        Const AUTO_RECONNECT_SO_IGNORE = $889E;
        {$DEFINE ASYNC_REQUEST_NOT_IN_USE}
        Const ASYNC_REQUEST_NOT_IN_USE = $889F;
        {$DEFINE AUTO_RECONNECT_FAILURE}
        Const AUTO_RECONNECT_FAILURE = $88A0;
        {$DEFINE NET_ERROR_ABORT_APPLICATION}
        Const NET_ERROR_ABORT_APPLICATION = $88A1;
        {$DEFINE NET_ERROR_SUSPEND_APPLICATION}
        Const NET_ERROR_SUSPEND_APPLICATION = $88A2;
        {$DEFINE NET_ERROR_ABORTED_PROCESS_GROUP}
        Const NET_ERROR_ABORTED_PROCESS_GROUP = $88A3;
        {$DEFINE NET_ERROR_PASSWORD_HAS_EXPIRED}
        Const NET_ERROR_PASSWORD_HAS_EXPIRED = $88A5;
        {$DEFINE NET_ERROR_NETWORK_INACTIVE}
        Const NET_ERROR_NETWORK_INACTIVE = $88A6;
        {$DEFINE REPLY_TRUNCATED}
        Const REPLY_TRUNCATED = $88e6;
        (* Server Errors *)
        {$DEFINE ERR_INSUFFICIENT_SPACE}
        Const ERR_INSUFFICIENT_SPACE = $8901;
        {$DEFINE ERR_NO_MORE_ENTRY}
        Const ERR_NO_MORE_ENTRY = $8914;
        {$DEFINE NLM_INVALID_CONNECTION}
        Const NLM_INVALID_CONNECTION = $890a;
        {$DEFINE ERR_BUFFER_TOO_SMALL}
        Const ERR_BUFFER_TOO_SMALL = $8977;
        {$DEFINE ERR_VOLUME_FLAG_NOT_SET}
        Const ERR_VOLUME_FLAG_NOT_SET = $8978;
        {$DEFINE ERR_NO_ITEMS_FOUND}
        Const ERR_NO_ITEMS_FOUND = $8979;
        {$DEFINE ERR_CONN_ALREADY_TEMP}
        Const ERR_CONN_ALREADY_TEMP = $897a;
        {$DEFINE ERR_CONN_ALREADY_LOGGED_IN}
        Const ERR_CONN_ALREADY_LOGGED_IN = $897b;
        {$DEFINE ERR_CONN_NOT_AUTHENTICATED}
        Const ERR_CONN_NOT_AUTHENTICATED = $897c;
        {$DEFINE ERR_CONN_NOT_LOGGED_IN}
        Const ERR_CONN_NOT_LOGGED_IN = $897d;
        {$DEFINE NCP_BOUNDARY_CHECK_FAILED}
        Const NCP_BOUNDARY_CHECK_FAILED = $897e;
        {$DEFINE ERR_LOCK_WAITING}
        Const ERR_LOCK_WAITING = $897f;
        {$DEFINE ERR_LOCK_FAIL}
        Const ERR_LOCK_FAIL = $8980;
        {$DEFINE FILE_IN_USE_ERROR}
        Const FILE_IN_USE_ERROR = $8980;
        {$DEFINE NO_MORE_FILE_HANDLES}
        Const NO_MORE_FILE_HANDLES = $8981;
        {$DEFINE NO_OPEN_PRIVILEGES}
        Const NO_OPEN_PRIVILEGES = $8982;
        {$DEFINE IO_ERROR_NETWORK_DISK}
        Const IO_ERROR_NETWORK_DISK = $8983;
        {$DEFINE NO_CREATE_PRIVILEGES}
        Const NO_CREATE_PRIVILEGES = $8984;
        {$DEFINE NO_CREATE_DELETE_PRIVILEGES}
        Const NO_CREATE_DELETE_PRIVILEGES = $8985;
        {$DEFINE CREATE_FILE_EXISTS_READ_ONLY}
        Const CREATE_FILE_EXISTS_READ_ONLY = $8986;
        {$DEFINE WILD_CARDS_IN_CREATE_FILE_NAME}
        Const WILD_CARDS_IN_CREATE_FILE_NAME = $8987;
        {$DEFINE CREATE_FILENAME_ERROR}
        Const CREATE_FILENAME_ERROR = $8987;
        {$DEFINE INVALID_FILE_HANDLE}
        Const INVALID_FILE_HANDLE = $8988;
        {$DEFINE NO_SEARCH_PRIVILEGES}
        Const NO_SEARCH_PRIVILEGES = $8989;
        {$DEFINE NO_DELETE_PRIVILEGES}
        Const NO_DELETE_PRIVILEGES = $898A;
        {$DEFINE NO_RENAME_PRIVILEGES}
        Const NO_RENAME_PRIVILEGES = $898B;
        {$DEFINE NO_MODIFY_PRIVILEGES}
        Const NO_MODIFY_PRIVILEGES = $898C;
        {$DEFINE SOME_FILES_AFFECTED_IN_USE}
        Const SOME_FILES_AFFECTED_IN_USE = $898D;
        {$DEFINE NO_FILES_AFFECTED_IN_USE}
        Const NO_FILES_AFFECTED_IN_USE = $898E;
        {$DEFINE SOME_FILES_AFFECTED_READ_ONLY}
        Const SOME_FILES_AFFECTED_READ_ONLY = $898F;
        {$DEFINE NO_FILES_AFFECTED_READ_ONLY}
        Const NO_FILES_AFFECTED_READ_ONLY = $8990;
        {$DEFINE SOME_FILES_RENAMED_NAME_EXISTS}
        Const SOME_FILES_RENAMED_NAME_EXISTS = $8991;
        {$DEFINE NO_FILES_RENAMED_NAME_EXISTS}
        Const NO_FILES_RENAMED_NAME_EXISTS = $8992;
        {$DEFINE NO_READ_PRIVILEGES}
        Const NO_READ_PRIVILEGES = $8993;
        {$DEFINE NO_WRITE_PRIVILEGES_OR_READONLY}
        Const NO_WRITE_PRIVILEGES_OR_READONLY = $8994;
        {$DEFINE FILE_DETACHED}
        Const FILE_DETACHED = $8995;
        {$DEFINE SERVER_OUT_OF_MEMORY}
        Const SERVER_OUT_OF_MEMORY = $8996;
        {$DEFINE ERR_TARGET_NOT_A_SUBDIRECTORY}
        Const ERR_TARGET_NOT_A_SUBDIRECTORY = $8996;
        {$DEFINE NO_DISK_SPACE_FOR_SPOOL_FILE}
        Const NO_DISK_SPACE_FOR_SPOOL_FILE = $8997;
        {$DEFINE VOLUME_DOES_NOT_EXIST}
        Const VOLUME_DOES_NOT_EXIST = $8998;
        {$DEFINE DIRECTORY_FULL}
        Const DIRECTORY_FULL = $8999;
        {$DEFINE RENAMING_ACROSS_VOLUMES}
        Const RENAMING_ACROSS_VOLUMES = $899A;
        {$DEFINE BAD_DIRECTORY_HANDLE}
        Const BAD_DIRECTORY_HANDLE = $899B;
        {$DEFINE INVALID_PATH}
        Const INVALID_PATH = $899C;
        {$DEFINE NO_MORE_TRUSTEES}
        Const NO_MORE_TRUSTEES = $899C;
        {$DEFINE NO_MORE_DIRECTORY_HANDLES}
        Const NO_MORE_DIRECTORY_HANDLES = $899D;
        {$DEFINE INVALID_FILENAME}
        Const INVALID_FILENAME = $899E;
        {$DEFINE DIRECTORY_ACTIVE}
        Const DIRECTORY_ACTIVE = $899F;
        {$DEFINE DIRECTORY_NOT_EMPTY}
        Const DIRECTORY_NOT_EMPTY = $89A0;
        {$DEFINE DIRECTORY_IO_ERROR}
        Const DIRECTORY_IO_ERROR = $89A1;
        {$DEFINE READ_FILE_WITH_RECORD_LOCKED}
        Const READ_FILE_WITH_RECORD_LOCKED = $89A2;
        {$DEFINE ERR_TRANSACTION_RESTARTED}
        Const ERR_TRANSACTION_RESTARTED = $89A3;
        {$DEFINE ERR_RENAME_DIR_INVALID}
        Const ERR_RENAME_DIR_INVALID = $89A4;
        {$DEFINE ERR_INVALID_OPENCREATE_MODE}
        Const ERR_INVALID_OPENCREATE_MODE = $89A5;
        {$DEFINE ERR_ALREADY_IN_USE}
        Const ERR_ALREADY_IN_USE = $89A6;
        {$DEFINE ERR_INVALID_RESOURCE_TAG}
        Const ERR_INVALID_RESOURCE_TAG = $89A7;
        {$DEFINE ERR_ACCESS_DENIED}
        Const ERR_ACCESS_DENIED = $89A8;
        {$DEFINE INVALID_DATA_STREAM}
        Const INVALID_DATA_STREAM = $89BE;
        {$DEFINE INVALID_NAME_SPACE}
        Const INVALID_NAME_SPACE = $89BF;
        {$DEFINE NO_ACCOUNTING_PRIVILEGES}
        Const NO_ACCOUNTING_PRIVILEGES = $89C0;
        {$DEFINE LOGIN_DENIED_NO_ACCOUNT_BALANCE}
        Const LOGIN_DENIED_NO_ACCOUNT_BALANCE = $89C1;
        {$DEFINE LOGIN_DENIED_NO_CREDIT}
        Const LOGIN_DENIED_NO_CREDIT = $89C2;
        {$DEFINE ERR_TOO_MANY_HOLDS}
        Const ERR_TOO_MANY_HOLDS = $89C3;
        {$DEFINE ACCOUNTING_DISABLED}
        Const ACCOUNTING_DISABLED = $89C4;
        {$DEFINE INTRUDER_DETECTION_LOCK}
        Const INTRUDER_DETECTION_LOCK = $89C5;
        {$DEFINE NO_CONSOLE_OPERATOR}
        Const NO_CONSOLE_OPERATOR = $89C6;
        {$DEFINE NO_CONSOLE_PRIVILEGES}
        Const NO_CONSOLE_PRIVILEGES = $89C6;
        {$DEFINE ERR_Q_IO_FAILURE}
        Const ERR_Q_IO_FAILURE = $89D0;
        {$DEFINE ERR_NO_QUEUE}
        Const ERR_NO_QUEUE = $89D1;
        {$DEFINE ERR_NO_Q_SERVER}
        Const ERR_NO_Q_SERVER = $89D2;
        {$DEFINE ERR_NO_Q_RIGHTS}
        Const ERR_NO_Q_RIGHTS = $89D3;
        {$DEFINE ERR_Q_FULL}
        Const ERR_Q_FULL = $89D4;
        {$DEFINE ERR_NO_Q_JOB}
        Const ERR_NO_Q_JOB = $89D5;
        {$DEFINE ERR_NO_Q_JOB_RIGHTS}
        Const ERR_NO_Q_JOB_RIGHTS = $89D6;
        {$DEFINE ERR_Q_IN_SERVICE}
        Const ERR_Q_IN_SERVICE = $89D7;
        {$DEFINE PASSWORD_NOT_UNIQUE}
        Const PASSWORD_NOT_UNIQUE = $89D7;
        {$DEFINE ERR_Q_NOT_ACTIVE}
        Const ERR_Q_NOT_ACTIVE = $89D8;
        {$DEFINE PASSWORD_TOO_SHORT}
        Const PASSWORD_TOO_SHORT = $89D8;
        {$DEFINE ERR_Q_STN_NOT_SERVER}
        Const ERR_Q_STN_NOT_SERVER = $89D9;
        {$DEFINE LOGIN_DENIED_NO_CONNECTION}
        Const LOGIN_DENIED_NO_CONNECTION = $89D9;
        {$DEFINE ERR_MAXIMUM_LOGINS_EXCEEDED}
        Const ERR_MAXIMUM_LOGINS_EXCEEDED = $89D9;
        {$DEFINE ERR_Q_HALTED}
        Const ERR_Q_HALTED = $89DA;
        {$DEFINE UNAUTHORIZED_LOGIN_TIME}
        Const UNAUTHORIZED_LOGIN_TIME = $89DA;
        {$DEFINE UNAUTHORIZED_LOGIN_STATION}
        Const UNAUTHORIZED_LOGIN_STATION = $89DB;
        {$DEFINE ERR_Q_MAX_SERVERS}
        Const ERR_Q_MAX_SERVERS = $89DB;
        {$DEFINE ACCOUNT_DISABLED}
        Const ACCOUNT_DISABLED = $89DC;
        {$DEFINE PASSWORD_HAS_EXPIRED_NO_GRACE}
        Const PASSWORD_HAS_EXPIRED_NO_GRACE = $89DE;
        {$DEFINE PASSWORD_HAS_EXPIRED}
        Const PASSWORD_HAS_EXPIRED = $89DF;
        {$DEFINE E_NO_MORE_USERS}
        Const E_NO_MORE_USERS = $89E7;
        {$DEFINE NOT_ITEM_PROPERTY}
        Const NOT_ITEM_PROPERTY = $89E8;
        {$DEFINE WRITE_PROPERTY_TO_GROUP}
        Const WRITE_PROPERTY_TO_GROUP = $89E8;
        {$DEFINE MEMBER_ALREADY_EXISTS}
        Const MEMBER_ALREADY_EXISTS = $89E9;
        {$DEFINE NO_SUCH_MEMBER}
        Const NO_SUCH_MEMBER = $89EA;
        {$DEFINE NOT_GROUP_PROPERTY}
        Const NOT_GROUP_PROPERTY = $89EB;
        {$DEFINE NO_SUCH_SEGMENT}
        Const NO_SUCH_SEGMENT = $89EC;
        {$DEFINE PROPERTY_ALREADY_EXISTS}
        Const PROPERTY_ALREADY_EXISTS = $89ED;
        {$DEFINE OBJECT_ALREADY_EXISTS}
        Const OBJECT_ALREADY_EXISTS = $89EE;
        {$DEFINE INVALID_NAME}
        Const INVALID_NAME = $89EF;
        {$DEFINE WILD_CARD_NOT_ALLOWED}
        Const WILD_CARD_NOT_ALLOWED = $89F0;
        {$DEFINE INVALID_BINDERY_SECURITY}
        Const INVALID_BINDERY_SECURITY = $89F1;
        {$DEFINE NO_OBJECT_READ_PRIVILEGE}
        Const NO_OBJECT_READ_PRIVILEGE = $89F2;
        {$DEFINE NO_OBJECT_RENAME_PRIVILEGE}
        Const NO_OBJECT_RENAME_PRIVILEGE = $89F3;
        {$DEFINE NO_OBJECT_DELETE_PRIVILEGE}
        Const NO_OBJECT_DELETE_PRIVILEGE = $89F4;
        {$DEFINE NO_OBJECT_CREATE_PRIVILEGE}
        Const NO_OBJECT_CREATE_PRIVILEGE = $89F5;
        {$DEFINE NO_PROPERTY_DELETE_PRIVILEGE}
        Const NO_PROPERTY_DELETE_PRIVILEGE = $89F6;
        {$DEFINE NO_PROPERTY_CREATE_PRIVILEGE}
        Const NO_PROPERTY_CREATE_PRIVILEGE = $89F7;
        {$DEFINE NO_PROPERTY_WRITE_PRIVILEGE}
        Const NO_PROPERTY_WRITE_PRIVILEGE = $89F8;
        {$DEFINE NO_FREE_CONNECTION_SLOTS}
        Const NO_FREE_CONNECTION_SLOTS = $89F9;
        {$DEFINE NO_PROPERTY_READ_PRIVILEGE}
        Const NO_PROPERTY_READ_PRIVILEGE = $89F9;
        {$DEFINE NO_MORE_SERVER_SLOTS}
        Const NO_MORE_SERVER_SLOTS = $89FA;
        {$DEFINE TEMP_REMAP_ERROR}
        Const TEMP_REMAP_ERROR = $89FA;
        {$DEFINE INVALID_PARAMETERS}
        Const INVALID_PARAMETERS = $89FB;
        {$DEFINE NO_SUCH_PROPERTY}
        Const NO_SUCH_PROPERTY = $89FB;
        {$DEFINE ERR_NCP_NOT_SUPPORTED}
        Const ERR_NCP_NOT_SUPPORTED = $89FB;
        {$DEFINE INTERNET_PACKET_REQT_CANCELED}
        Const INTERNET_PACKET_REQT_CANCELED = $89FC;
        {$DEFINE UNKNOWN_FILE_SERVER}
        Const UNKNOWN_FILE_SERVER = $89FC;
        {$DEFINE MESSAGE_QUEUE_FULL}
        Const MESSAGE_QUEUE_FULL = $89FC;
        {$DEFINE NO_SUCH_OBJECT}
        Const NO_SUCH_OBJECT = $89FC;
        {$DEFINE LOCK_COLLISION}
        Const LOCK_COLLISION = $89FD;
        {$DEFINE BAD_STATION_NUMBER}
        Const BAD_STATION_NUMBER = $89FD;
        {$DEFINE INVALID_PACKET_LENGTH}
        Const INVALID_PACKET_LENGTH = $89FD;
        {$DEFINE UNKNOWN_REQUEST}
        Const UNKNOWN_REQUEST = $89FD;
        {$DEFINE BINDERY_LOCKED}
        Const BINDERY_LOCKED = $89FE;
        {$DEFINE TRUSTEE_NOT_FOUND}
        Const TRUSTEE_NOT_FOUND = $89FE;
        {$DEFINE DIRECTORY_LOCKED}
        Const DIRECTORY_LOCKED = $89FE;
        {$DEFINE INVALID_SEMAPHORE_NAME_LENGTH}
        Const INVALID_SEMAPHORE_NAME_LENGTH = $89FE;
        {$DEFINE PACKET_NOT_DELIVERABLE}
        Const PACKET_NOT_DELIVERABLE = $89FE;
        {$DEFINE SERVER_BINDERY_LOCKED}
        Const SERVER_BINDERY_LOCKED = $89FE;
        {$DEFINE SOCKET_TABLE_FULL}
        Const SOCKET_TABLE_FULL = $89FE;
        {$DEFINE SPOOL_DIRECTORY_ERROR}
        Const SPOOL_DIRECTORY_ERROR = $89FE;
        {$DEFINE SUPERVISOR_HAS_DISABLED_LOGIN}
        Const SUPERVISOR_HAS_DISABLED_LOGIN = $89FE;
        {$DEFINE TIMEOUT_FAILURE}
        Const TIMEOUT_FAILURE = $89FE;
        {$DEFINE BAD_PRINTER_ERROR}
        Const BAD_PRINTER_ERROR = $89FF;
        {$DEFINE BAD_RECORD_OFFSET}
        Const BAD_RECORD_OFFSET = $89FF;
        {$DEFINE CLOSE_FCB_ERROR}
        Const CLOSE_FCB_ERROR = $89FF;
        {$DEFINE FILE_EXTENSION_ERROR}
        Const FILE_EXTENSION_ERROR = $89FF;
        {$DEFINE FILE_NAME_ERROR}
        Const FILE_NAME_ERROR = $89FF;
        {$DEFINE HARDWARE_FAILURE}
        Const HARDWARE_FAILURE = $89FF;
        {$DEFINE INVALID_DRIVE_NUMBER}
        Const INVALID_DRIVE_NUMBER = $89FF;
        {$DEFINE DOS_INVALID_DRIVE}
        Const DOS_INVALID_DRIVE = $000F;
        {$DEFINE INVALID_INITIAL_SEMAPHORE_VALUE}
        Const INVALID_INITIAL_SEMAPHORE_VALUE = $89FF;
        {$DEFINE INVALID_SEMAPHORE_HANDLE}
        Const INVALID_SEMAPHORE_HANDLE = $89FF;
        {$DEFINE IO_BOUND_ERROR}
        Const IO_BOUND_ERROR = $89FF;
        {$DEFINE NO_FILES_FOUND_ERROR}
        Const NO_FILES_FOUND_ERROR = $89FF;
        {$DEFINE NO_RESPONSE_FROM_SERVER}
        Const NO_RESPONSE_FROM_SERVER = $89FF;
        {$DEFINE NO_SUCH_OBJECT_OR_BAD_PASSWORD}
        Const NO_SUCH_OBJECT_OR_BAD_PASSWORD = $89FF;
        {$DEFINE PATH_NOT_LOCATABLE}
        Const PATH_NOT_LOCATABLE = $89FF;
        {$DEFINE QUEUE_FULL_ERROR}
        Const QUEUE_FULL_ERROR = $89FF;
        {$DEFINE REQUEST_NOT_OUTSTANDING}
        Const REQUEST_NOT_OUTSTANDING = $89FF;
        {$DEFINE SOCKET_ALREADY_OPEN}
        Const SOCKET_ALREADY_OPEN = $89FF;
        {$DEFINE LOCK_ERROR}
        Const LOCK_ERROR = $89FF;
        {$DEFINE FAILURE}
        Const FAILURE = $89FF;
        (* #define NOT_SAME_LOCAL_DRIVE         0x89F6 *)
        (* #define TARGET_DRIVE_NOT_LOCAL       0x89F7 *)
        (* #define ALREADY_ATTACHED_TO_SERVER   0x89F8 *)
        (* 248 *)
        (* #define NOT_ATTACHED_TO_SERVER       0x89F8 *)
        (**** Network errors ****)
        (* Decimal values at end of line are 32768 lower than actual *)
        {$DEFINE NWE_ALREADY_ATTACHED}
        Const NWE_ALREADY_ATTACHED = $8800;
        {$DEFINE NWE_CONN_INVALID}
        Const NWE_CONN_INVALID = $8801;
        {$DEFINE NWE_DRIVE_IN_USE}
        Const NWE_DRIVE_IN_USE = $8802;
        {$DEFINE NWE_DRIVE_CANNOT_MAP}
        Const NWE_DRIVE_CANNOT_MAP = $8803;
        {$DEFINE NWE_DRIVE_BAD_PATH}
        Const NWE_DRIVE_BAD_PATH = $8804;
        {$DEFINE NWE_NET_RECEIVE}
        Const NWE_NET_RECEIVE = $8805;
        {$DEFINE NWE_NET_UNKNOWN}
        Const NWE_NET_UNKNOWN = $8806;
        {$DEFINE NWE_SERVER_BAD_SLOT}
        Const NWE_SERVER_BAD_SLOT = $8807;
        {$DEFINE NWE_SERVER_NO_SLOTS}
        Const NWE_SERVER_NO_SLOTS = $8808;
        {$DEFINE NWE_NET_SEND}
        Const NWE_NET_SEND = $8809;
        {$DEFINE NWE_SERVER_NO_ROUTE}
        Const NWE_SERVER_NO_ROUTE = $880A;
        {$DEFINE NWE_BAD_LOCAL_TARGET}
        Const NWE_BAD_LOCAL_TARGET = $880B;
        {$DEFINE NWE_REQ_TOO_MANY_REQ_FRAGS}
        Const NWE_REQ_TOO_MANY_REQ_FRAGS = $880C;
        {$DEFINE NWE_CONN_LIST_OVERFLOW}
        Const NWE_CONN_LIST_OVERFLOW = $880D;
        {$DEFINE NWE_BUFFER_OVERFLOW}
        Const NWE_BUFFER_OVERFLOW = $880E;
        {$DEFINE NWE_SERVER_NO_CONN}
        Const NWE_SERVER_NO_CONN = $880F;
        {$DEFINE NWE_NO_ROUTER_FOUND}
        Const NWE_NO_ROUTER_FOUND = $8810;
        {$DEFINE NWE_FUNCTION_INVALID}
        Const NWE_FUNCTION_INVALID = $8811;
        {$DEFINE NWE_SCAN_COMPLETE}
        Const NWE_SCAN_COMPLETE = $8812;
        {$DEFINE NWE_UNSUPPORTED_NAME_FORMAT_TYP}
        Const NWE_UNSUPPORTED_NAME_FORMAT_TYP = $8813;
        {$DEFINE NWE_HANDLE_ALREADY_LICENSED}
        Const NWE_HANDLE_ALREADY_LICENSED = $8814;
        {$DEFINE NWE_HANDLE_ALREADY_UNLICENSED}
        Const NWE_HANDLE_ALREADY_UNLICENSED = $8815;
        {$DEFINE NWE_INVALID_NCP_PACKET_LENGTH}
        Const NWE_INVALID_NCP_PACKET_LENGTH = $8816;
        {$DEFINE NWE_SETTING_UP_TIMEOUT}
        Const NWE_SETTING_UP_TIMEOUT = $8817;
        {$DEFINE NWE_SETTING_SIGNALS}
        Const NWE_SETTING_SIGNALS = $8818;
        {$DEFINE NWE_SERVER_CONNECTION_LOST}
        Const NWE_SERVER_CONNECTION_LOST = $8819;
        {$DEFINE NWE_OUT_OF_HEAP_SPACE}
        Const NWE_OUT_OF_HEAP_SPACE = $881A;
        {$DEFINE NWE_INVALID_SERVICE_REQUEST}
        Const NWE_INVALID_SERVICE_REQUEST = $881B;
        {$DEFINE NWE_INVALID_TASK_NUMBER}
        Const NWE_INVALID_TASK_NUMBER = $881C;
        {$DEFINE NWE_INVALID_MESSAGE_LENGTH}
        Const NWE_INVALID_MESSAGE_LENGTH = $881D;
        {$DEFINE NWE_EA_SCAN_DONE}
        Const NWE_EA_SCAN_DONE = $881E;
        {$DEFINE NWE_BAD_CONNECTION_NUMBER}
        Const NWE_BAD_CONNECTION_NUMBER = $881F;
        {$DEFINE NWE_MULT_TREES_NOT_SUPPORTED}
        Const NWE_MULT_TREES_NOT_SUPPORTED = $8820;
        {$DEFINE NWE_CONN_NOT_SAME}
        Const NWE_CONN_NOT_SAME = $8830;
        {$DEFINE NWE_CONN_PRIMARY_NOT_SET}
        Const NWE_CONN_PRIMARY_NOT_SET = $8831;
        {$DEFINE NWE_PRN_CAPTURE_NOT_IN_PROGRESS}
        Const NWE_PRN_CAPTURE_NOT_IN_PROGRESS = $8832;
        {$DEFINE NWE_BUFFER_INVALID_LEN}
        Const NWE_BUFFER_INVALID_LEN = $8833;
        {$DEFINE NWE_USER_NO_NAME}
        Const NWE_USER_NO_NAME = $8834;
        {$DEFINE NWE_PRN_NO_LOCAL_SPOOLER}
        Const NWE_PRN_NO_LOCAL_SPOOLER = $8835;
        {$DEFINE NWE_PARAM_INVALID}
        Const NWE_PARAM_INVALID = $8836;
        {$DEFINE NWE_CFG_OPEN_FAILED}
        Const NWE_CFG_OPEN_FAILED = $8837;
        {$DEFINE NWE_CFG_NO_FILE}
        Const NWE_CFG_NO_FILE = $8838;
        {$DEFINE NWE_CFG_READ_FAILED}
        Const NWE_CFG_READ_FAILED = $8839;
        {$DEFINE NWE_CFG_LINE_TOO_LONG}
        Const NWE_CFG_LINE_TOO_LONG = $883A;
        {$DEFINE NWE_CFG_LINES_IGNORED}
        Const NWE_CFG_LINES_IGNORED = $883B;
        {$DEFINE NWE_RESOURCE_NOT_OWNED}
        Const NWE_RESOURCE_NOT_OWNED = $883C;
        {$DEFINE NWE_DAEMON_INSTALLED}
        Const NWE_DAEMON_INSTALLED = $883D;
        {$DEFINE NWE_PRN_SPOOLER_INSTALLED}
        Const NWE_PRN_SPOOLER_INSTALLED = $883E;
        {$DEFINE NWE_CONN_TABLE_FULL}
        Const NWE_CONN_TABLE_FULL = $883F;
        {$DEFINE NWE_CFG_SECTION_NOT_FOUND}
        Const NWE_CFG_SECTION_NOT_FOUND = $8840;
        {$DEFINE NWE_TRAN_INVALID_TYPE}
        Const NWE_TRAN_INVALID_TYPE = $8841;
        {$DEFINE NWE_TDS_TAG_IN_USE}
        Const NWE_TDS_TAG_IN_USE = $8842;
        {$DEFINE NWE_TDS_OUT_OF_MEMORY}
        Const NWE_TDS_OUT_OF_MEMORY = $8843;
        {$DEFINE NWE_TDS_INVALID_TAG}
        Const NWE_TDS_INVALID_TAG = $8844;
        {$DEFINE NWE_TDS_WRITE_TRUNCATED}
        Const NWE_TDS_WRITE_TRUNCATED = $8845;
        {$DEFINE NWE_DS_NO_CONN}
        Const NWE_DS_NO_CONN = $8846;
        {$DEFINE NWE_SERVICE_BUSY}
        Const NWE_SERVICE_BUSY = $8846;
        {$DEFINE NWE_SERVER_NOT_FOUND}
        Const NWE_SERVER_NOT_FOUND = $8847;
        {$DEFINE NWE_VLM_INVALID}
        Const NWE_VLM_INVALID = $8848;
        {$DEFINE NWE_DRIVE_ALREADY_MAPPED}
        Const NWE_DRIVE_ALREADY_MAPPED = $8849;
        {$DEFINE NWE_DRIVE_LOCAL_IN_USE}
        Const NWE_DRIVE_LOCAL_IN_USE = $884A;
        {$DEFINE NWE_DRIVE_NONE_AVAILABLE}
        Const NWE_DRIVE_NONE_AVAILABLE = $884B;
        {$DEFINE NWE_DEVICE_NOT_REDIRECTED}
        Const NWE_DEVICE_NOT_REDIRECTED = $884C;
        {$DEFINE NWE_FILE_MAX_REACHED}
        Const NWE_FILE_MAX_REACHED = $884D;
        {$DEFINE NWE_UNLOAD_FAILED}
        Const NWE_UNLOAD_FAILED = $884E;
        {$DEFINE NWE_CONN_IN_USE}
        Const NWE_CONN_IN_USE = $884F;
        {$DEFINE NWE_REQ_TOO_MANY_REP_FRAGS}
        Const NWE_REQ_TOO_MANY_REP_FRAGS = $8850;
        {$DEFINE NWE_NAME_TABLE_FULL}
        Const NWE_NAME_TABLE_FULL = $8851;
        {$DEFINE NWE_SOCKET_NOT_OPEN}
        Const NWE_SOCKET_NOT_OPEN = $8852;
        {$DEFINE NWE_MEMORY_MGR_ERROR}
        Const NWE_MEMORY_MGR_ERROR = $8853;
        {$DEFINE NWE_SFT3_ERROR}
        Const NWE_SFT3_ERROR = $8854;
        {$DEFINE NWE_DS_PREFERRED_NOT_FOUND}
        Const NWE_DS_PREFERRED_NOT_FOUND = $8855;
        {$DEFINE NWE_DEVICE_NOT_RECOGNIZED}
        Const NWE_DEVICE_NOT_RECOGNIZED = $8856;
        {$DEFINE NWE_NET_INVALID_TYPE}
        Const NWE_NET_INVALID_TYPE = $8857;
        {$DEFINE NWE_FILE_OPEN_FAILED}
        Const NWE_FILE_OPEN_FAILED = $8858;
        {$DEFINE NWE_DS_PREFERRED_NOT_SPECIFIED}
        Const NWE_DS_PREFERRED_NOT_SPECIFIED = $8859;
        {$DEFINE NWE_SOCKET_OPEN_FAILED}
        Const NWE_SOCKET_OPEN_FAILED = $885A;
        {$DEFINE NWE_SIGNATURE_LEVEL_CONFLICT}
        Const NWE_SIGNATURE_LEVEL_CONFLICT = $8861;
        {$DEFINE NWE_NO_LOCK_FOUND}
        Const NWE_NO_LOCK_FOUND = $8862;
        {$DEFINE NWE_LOCK_TABLE_FULL}
        Const NWE_LOCK_TABLE_FULL = $8863;
        {$DEFINE NWE_INVALID_MATCH_DATA}
        Const NWE_INVALID_MATCH_DATA = $8864;
        {$DEFINE NWE_MATCH_FAILED}
        Const NWE_MATCH_FAILED = $8865;
        {$DEFINE NWE_NO_MORE_ENTRIES}
        Const NWE_NO_MORE_ENTRIES = $8866;
        {$DEFINE NWE_INSUFFICIENT_RESOURCES}
        Const NWE_INSUFFICIENT_RESOURCES = $8867;
        {$DEFINE NWE_STRING_TRANSLATION}
        Const NWE_STRING_TRANSLATION = $8868;
        {$DEFINE NWE_ACCESS_VIOLATION}
        Const NWE_ACCESS_VIOLATION = $8869;
        {$DEFINE NWE_NOT_AUTHENTICATED}
        Const NWE_NOT_AUTHENTICATED = $886A;
        {$DEFINE NWE_INVALID_LEVEL}
        Const NWE_INVALID_LEVEL = $886B;
        {$DEFINE NWE_RESOURCE_LOCK}
        Const NWE_RESOURCE_LOCK = $886C;
        {$DEFINE NWE_INVALID_NAME_FORMAT}
        Const NWE_INVALID_NAME_FORMAT = $886D;
        {$DEFINE NWE_OBJECT_EXISTS}
        Const NWE_OBJECT_EXISTS = $886E;
        {$DEFINE NWE_OBJECT_NOT_FOUND}
        Const NWE_OBJECT_NOT_FOUND = $886F;
        {$DEFINE NWE_UNSUPPORTED_TRAN_TYPE}
        Const NWE_UNSUPPORTED_TRAN_TYPE = $8870;
        {$DEFINE NWE_INVALID_STRING_TYPE}
        Const NWE_INVALID_STRING_TYPE = $8871;
        {$DEFINE NWE_INVALID_OWNER}
        Const NWE_INVALID_OWNER = $8872;
        {$DEFINE NWE_UNSUPPORTED_AUTHENTICATOR}
        Const NWE_UNSUPPORTED_AUTHENTICATOR = $8873;
        {$DEFINE NWE_IO_PENDING}
        Const NWE_IO_PENDING = $8874;
        {$DEFINE NWE_INVALID_DRIVE_NUMBER}
        Const NWE_INVALID_DRIVE_NUMBER = $8875;
        {$DEFINE NWE_REPLY_TRUNCATED}
        Const NWE_REPLY_TRUNCATED = $88e6;
        {$DEFINE NWE_REQUESTER_FAILURE}
        Const NWE_REQUESTER_FAILURE = $88FF;
        (* Server Errors *)
        {$DEFINE NWE_INSUFFICIENT_SPACE}
        Const NWE_INSUFFICIENT_SPACE = $8901;
        {$DEFINE NWE_BUFFER_TOO_SMALL}
        Const NWE_BUFFER_TOO_SMALL = $8977;
        {$DEFINE NWE_VOL_FLAG_NOT_SET}
        Const NWE_VOL_FLAG_NOT_SET = $8978;
        {$DEFINE NWE_NO_ITEMS_FOUND}
        Const NWE_NO_ITEMS_FOUND = $8979;
        {$DEFINE NWE_CONN_ALREADY_TEMP}
        Const NWE_CONN_ALREADY_TEMP = $897a;
        {$DEFINE NWE_CONN_ALREADY_LOGGED_IN}
        Const NWE_CONN_ALREADY_LOGGED_IN = $897b;
        {$DEFINE NWE_CONN_NOT_AUTHENTICATED}
        Const NWE_CONN_NOT_AUTHENTICATED = $897c;
        {$DEFINE NWE_CONN_NOT_LOGGED_IN}
        Const NWE_CONN_NOT_LOGGED_IN = $897d;
        {$DEFINE NWE_NCP_BOUNDARY_CHECK_FAILED}
        Const NWE_NCP_BOUNDARY_CHECK_FAILED = $897e;
        {$DEFINE NWE_LOCK_WAITING}
        Const NWE_LOCK_WAITING = $897f;
        {$DEFINE NWE_LOCK_FAIL}
        Const NWE_LOCK_FAIL = $8980;
        {$DEFINE NWE_FILE_IN_USE}
        Const NWE_FILE_IN_USE = $8980;
        {$DEFINE NWE_FILE_NO_HANDLES}
        Const NWE_FILE_NO_HANDLES = $8981;
        {$DEFINE NWE_FILE_NO_OPEN_PRIV}
        Const NWE_FILE_NO_OPEN_PRIV = $8982;
        {$DEFINE NWE_DISK_IO_ERROR}
        Const NWE_DISK_IO_ERROR = $8983;
        {$DEFINE NWE_FILE_NO_CREATE_PRIV}
        Const NWE_FILE_NO_CREATE_PRIV = $8984;
        {$DEFINE NWE_FILE_NO_CREATE_DEL_PRIV}
        Const NWE_FILE_NO_CREATE_DEL_PRIV = $8985;
        {$DEFINE NWE_FILE_EXISTS_READ_ONLY}
        Const NWE_FILE_EXISTS_READ_ONLY = $8986;
        {$DEFINE NWE_FILE_WILD_CARDS_IN_NAME}
        Const NWE_FILE_WILD_CARDS_IN_NAME = $8987;
        {$DEFINE NWE_FILE_INVALID_HANDLE}
        Const NWE_FILE_INVALID_HANDLE = $8988;
        {$DEFINE NWE_FILE_NO_SRCH_PRIV}
        Const NWE_FILE_NO_SRCH_PRIV = $8989;
        {$DEFINE NWE_FILE_NO_DEL_PRIV}
        Const NWE_FILE_NO_DEL_PRIV = $898A;
        {$DEFINE NWE_FILE_NO_RENAME_PRIV}
        Const NWE_FILE_NO_RENAME_PRIV = $898B;
        {$DEFINE NWE_FILE_NO_MOD_PRIV}
        Const NWE_FILE_NO_MOD_PRIV = $898C;
        {$DEFINE NWE_FILE_SOME_IN_USE}
        Const NWE_FILE_SOME_IN_USE = $898D;
        {$DEFINE NWE_FILE_NONE_IN_USE}
        Const NWE_FILE_NONE_IN_USE = $898E;
        {$DEFINE NWE_FILE_SOME_READ_ONLY}
        Const NWE_FILE_SOME_READ_ONLY = $898F;
        {$DEFINE NWE_FILE_NONE_READ_ONLY}
        Const NWE_FILE_NONE_READ_ONLY = $8990;
        {$DEFINE NWE_FILE_SOME_RENAMED_EXIST}
        Const NWE_FILE_SOME_RENAMED_EXIST = $8991;
        {$DEFINE NWE_FILE_NONE_RENAMED_EXIST}
        Const NWE_FILE_NONE_RENAMED_EXIST = $8992;
        {$DEFINE NWE_FILE_NO_READ_PRIV}
        Const NWE_FILE_NO_READ_PRIV = $8993;
        {$DEFINE NWE_FILE_NO_WRITE_PRIV}
        Const NWE_FILE_NO_WRITE_PRIV = $8994;
        {$DEFINE NWE_FILE_READ_ONLY}
        Const NWE_FILE_READ_ONLY = $8994;
        {$DEFINE NWE_FILE_DETACHED}
        Const NWE_FILE_DETACHED = $8995;
        {$DEFINE NWE_SERVER_OUT_OF_MEMORY}
        Const NWE_SERVER_OUT_OF_MEMORY = $8996;
        {$DEFINE NWE_DIR_TARGET_INVALID}
        Const NWE_DIR_TARGET_INVALID = $8996;
        {$DEFINE NWE_DISK_NO_SPOOL_SPACE}
        Const NWE_DISK_NO_SPOOL_SPACE = $8997;
        {$DEFINE NWE_VOL_INVALID}
        Const NWE_VOL_INVALID = $8998;
        {$DEFINE NWE_DIR_FULL}
        Const NWE_DIR_FULL = $8999;
        {$DEFINE NWE_VOL_RENAMING_ACROSS}
        Const NWE_VOL_RENAMING_ACROSS = $899A;
        {$DEFINE NWE_DIRHANDLE_INVALID}
        Const NWE_DIRHANDLE_INVALID = $899B;
        {$DEFINE NWE_PATH_INVALID}
        Const NWE_PATH_INVALID = $899C;
        {$DEFINE NWE_TRUSTEES_NO_MORE}
        Const NWE_TRUSTEES_NO_MORE = $899C;
        {$DEFINE NWE_DIRHANDLE_NO_MORE}
        Const NWE_DIRHANDLE_NO_MORE = $899D;
        {$DEFINE NWE_FILE_NAME_INVALID}
        Const NWE_FILE_NAME_INVALID = $899E;
        {$DEFINE NWE_DIR_ACTIVE}
        Const NWE_DIR_ACTIVE = $899F;
        {$DEFINE NWE_DIR_NOT_EMPTY}
        Const NWE_DIR_NOT_EMPTY = $89A0;
        {$DEFINE NWE_DIR_IO_ERROR}
        Const NWE_DIR_IO_ERROR = $89A1;
        {$DEFINE NWE_FILE_IO_LOCKED}
        Const NWE_FILE_IO_LOCKED = $89A2;
        {$DEFINE NWE_TTS_RANSACTION_RESTARTED}
        Const NWE_TTS_RANSACTION_RESTARTED = $89A3;
        {$DEFINE NWE_TTS_TRANSACTION_RESTARTED}
        Const NWE_TTS_TRANSACTION_RESTARTED = $89A3;
        {$DEFINE NWE_DIR_RENAME_INVALID}
        Const NWE_DIR_RENAME_INVALID = $89A4;
        {$DEFINE NWE_FILE_OPENCREAT_MODE_INVALID}
        Const NWE_FILE_OPENCREAT_MODE_INVALID = $89A5;
        {$DEFINE NWE_ALREADY_IN_USE}
        Const NWE_ALREADY_IN_USE = $89A6;
        {$DEFINE NWE_RESOURCE_TAG_INVALID}
        Const NWE_RESOURCE_TAG_INVALID = $89A7;
        {$DEFINE NWE_ACCESS_DENIED}
        Const NWE_ACCESS_DENIED = $89A8;
        {$DEFINE NWE_DATA_STREAM_INVALID}
        Const NWE_DATA_STREAM_INVALID = $89BE;
        {$DEFINE NWE_NAME_SPACE_INVALID}
        Const NWE_NAME_SPACE_INVALID = $89BF;
        {$DEFINE NWE_ACCTING_NO_PRIV}
        Const NWE_ACCTING_NO_PRIV = $89C0;
        {$DEFINE NWE_ACCTING_NO_BALANCE}
        Const NWE_ACCTING_NO_BALANCE = $89C1;
        {$DEFINE NWE_ACCTING_NO_CREDIT}
        Const NWE_ACCTING_NO_CREDIT = $89C2;
        {$DEFINE NWE_ACCTING_TOO_MANY_HOLDS}
        Const NWE_ACCTING_TOO_MANY_HOLDS = $89C3;
        {$DEFINE NWE_ACCTING_DISABLED}
        Const NWE_ACCTING_DISABLED = $89C4;
        {$DEFINE NWE_LOGIN_LOCKOUT}
        Const NWE_LOGIN_LOCKOUT = $89C5;
        {$DEFINE NWE_CONSOLE_NO_PRIV}
        Const NWE_CONSOLE_NO_PRIV = $89C6;
        {$DEFINE NWE_Q_IO_FAILURE}
        Const NWE_Q_IO_FAILURE = $89D0;
        {$DEFINE NWE_Q_NONE}
        Const NWE_Q_NONE = $89D1;
        {$DEFINE NWE_Q_NO_SERVER}
        Const NWE_Q_NO_SERVER = $89D2;
        {$DEFINE NWE_Q_NO_RIGHTS}
        Const NWE_Q_NO_RIGHTS = $89D3;
        {$DEFINE NWE_Q_FULL}
        Const NWE_Q_FULL = $89D4;
        {$DEFINE NWE_Q_NO_JOB}
        Const NWE_Q_NO_JOB = $89D5;
        {$DEFINE NWE_Q_NO_JOB_RIGHTS}
        Const NWE_Q_NO_JOB_RIGHTS = $89D6;
        {$DEFINE NWE_PASSWORD_UNENCRYPTED}
        Const NWE_PASSWORD_UNENCRYPTED = $89D6;
        {$DEFINE NWE_Q_IN_SERVICE}
        Const NWE_Q_IN_SERVICE = $89D7;
        {$DEFINE NWE_PASSWORD_NOT_UNIQUE}
        Const NWE_PASSWORD_NOT_UNIQUE = $89D7;
        {$DEFINE NWE_Q_NOT_ACTIVE}
        Const NWE_Q_NOT_ACTIVE = $89D8;
        {$DEFINE NWE_PASSWORD_TOO_SHORT}
        Const NWE_PASSWORD_TOO_SHORT = $89D8;
        {$DEFINE NWE_Q_STN_NOT_SERVER}
        Const NWE_Q_STN_NOT_SERVER = $89D9;
        {$DEFINE NWE_LOGIN_NO_CONN}
        Const NWE_LOGIN_NO_CONN = $89D9;
        {$DEFINE NWE_LOGIN_MAX_EXCEEDED}
        Const NWE_LOGIN_MAX_EXCEEDED = $89D9;
        {$DEFINE NWE_Q_HALTED}
        Const NWE_Q_HALTED = $89DA;
        {$DEFINE NWE_LOGIN_UNAUTHORIZED_TIME}
        Const NWE_LOGIN_UNAUTHORIZED_TIME = $89DA;
        {$DEFINE NWE_LOGIN_UNAUTHORIZED_STATION}
        Const NWE_LOGIN_UNAUTHORIZED_STATION = $89DB;
        {$DEFINE NWE_Q_MAX_SERVERS}
        Const NWE_Q_MAX_SERVERS = $89DB;
        {$DEFINE NWE_ACCT_DISABLED}
        Const NWE_ACCT_DISABLED = $89DC;
        {$DEFINE NWE_PASSWORD_INVALID}
        Const NWE_PASSWORD_INVALID = $89DE;
        {$DEFINE NWE_PASSWORD_EXPIRED}
        Const NWE_PASSWORD_EXPIRED = $89DF;
        {$DEFINE NWE_LOGIN_NO_CONN_AVAIL}
        Const NWE_LOGIN_NO_CONN_AVAIL = $89E0;
        {$DEFINE NWE_E_NO_MORE_USERS}
        Const NWE_E_NO_MORE_USERS = $89E7;
        {$DEFINE NWE_BIND_NOT_ITEM_PROP}
        Const NWE_BIND_NOT_ITEM_PROP = $89E8;
        {$DEFINE NWE_BIND_WRITE_TO_GROUP_PROP}
        Const NWE_BIND_WRITE_TO_GROUP_PROP = $89E8;
        {$DEFINE NWE_BIND_MEMBER_ALREADY_EXISTS}
        Const NWE_BIND_MEMBER_ALREADY_EXISTS = $89E9;
        {$DEFINE NWE_BIND_NO_SUCH_MEMBER}
        Const NWE_BIND_NO_SUCH_MEMBER = $89EA;
        {$DEFINE NWE_BIND_NOT_GROUP_PROP}
        Const NWE_BIND_NOT_GROUP_PROP = $89EB;
        {$DEFINE NWE_BIND_NO_SUCH_SEGMENT}
        Const NWE_BIND_NO_SUCH_SEGMENT = $89EC;
        {$DEFINE NWE_BIND_PROP_ALREADY_EXISTS}
        Const NWE_BIND_PROP_ALREADY_EXISTS = $89ED;
        {$DEFINE NWE_BIND_OBJ_ALREADY_EXISTS}
        Const NWE_BIND_OBJ_ALREADY_EXISTS = $89EE;
        {$DEFINE NWE_BIND_NAME_INVALID}
        Const NWE_BIND_NAME_INVALID = $89EF;
        {$DEFINE NWE_BIND_WILDCARD_INVALID}
        Const NWE_BIND_WILDCARD_INVALID = $89F0;
        {$DEFINE NWE_BIND_SECURITY_INVALID}
        Const NWE_BIND_SECURITY_INVALID = $89F1;
        {$DEFINE NWE_BIND_OBJ_NO_READ_PRIV}
        Const NWE_BIND_OBJ_NO_READ_PRIV = $89F2;
        {$DEFINE NWE_BIND_OBJ_NO_RENAME_PRIV}
        Const NWE_BIND_OBJ_NO_RENAME_PRIV = $89F3;
        {$DEFINE NWE_BIND_OBJ_NO_DELETE_PRIV}
        Const NWE_BIND_OBJ_NO_DELETE_PRIV = $89F4;
        {$DEFINE NWE_BIND_OBJ_NO_CREATE_PRIV}
        Const NWE_BIND_OBJ_NO_CREATE_PRIV = $89F5;
        {$DEFINE NWE_BIND_PROP_NO_DELETE_PRIV}
        Const NWE_BIND_PROP_NO_DELETE_PRIV = $89F6;
        {$DEFINE NWE_BIND_PROP_NO_CREATE_PRIV}
        Const NWE_BIND_PROP_NO_CREATE_PRIV = $89F7;
        {$DEFINE NWE_BIND_PROP_NO_WRITE_PRIV}
        Const NWE_BIND_PROP_NO_WRITE_PRIV = $89F8;
        {$DEFINE NWE_BIND_PROP_NO_READ_PRIV}
        Const NWE_BIND_PROP_NO_READ_PRIV = $89F9;
        {$DEFINE NWE_NO_FREE_CONN_SLOTS}
        Const NWE_NO_FREE_CONN_SLOTS = $89F9;
        {$DEFINE NWE_NO_MORE_SERVER_SLOTS}
        Const NWE_NO_MORE_SERVER_SLOTS = $89FA;
        {$DEFINE NWE_TEMP_REMAP_ERROR}
        Const NWE_TEMP_REMAP_ERROR = $89FA;
        {$DEFINE NWE_PARAMETERS_INVALID}
        Const NWE_PARAMETERS_INVALID = $89FB;
        {$DEFINE NWE_BIND_NO_SUCH_PROP}
        Const NWE_BIND_NO_SUCH_PROP = $89FB;
        {$DEFINE NWE_NCP_NOT_SUPPORTED}
        Const NWE_NCP_NOT_SUPPORTED = $89FB;
        {$DEFINE NWE_INET_PACKET_REQ_CANCELED}
        Const NWE_INET_PACKET_REQ_CANCELED = $89FC;
        {$DEFINE NWE_SERVER_UNKNOWN}
        Const NWE_SERVER_UNKNOWN = $89FC;
        {$DEFINE NWE_MSG_Q_FULL}
        Const NWE_MSG_Q_FULL = $89FC;
        {$DEFINE NWE_BIND_NO_SUCH_OBJ}
        Const NWE_BIND_NO_SUCH_OBJ = $89FC;
        {$DEFINE NWE_LOCK_COLLISION}
        Const NWE_LOCK_COLLISION = $89FD;
        {$DEFINE NWE_CONN_NUM_INVALID}
        Const NWE_CONN_NUM_INVALID = $89FD;
        {$DEFINE NWE_PACKET_LEN_INVALID}
        Const NWE_PACKET_LEN_INVALID = $89FD;
        {$DEFINE NWE_UNKNOWN_REQ}
        Const NWE_UNKNOWN_REQ = $89FD;
        {$DEFINE NWE_BIND_LOCKED}
        Const NWE_BIND_LOCKED = $89FE;
        {$DEFINE NWE_TRUSTEE_NOT_FOUND}
        Const NWE_TRUSTEE_NOT_FOUND = $89FE;
        {$DEFINE NWE_DIR_LOCKED}
        Const NWE_DIR_LOCKED = $89FE;
        {$DEFINE NWE_SEM_INVALID_NAME_LEN}
        Const NWE_SEM_INVALID_NAME_LEN = $89FE;
        {$DEFINE NWE_PACKET_NOT_DELIVERABLE}
        Const NWE_PACKET_NOT_DELIVERABLE = $89FE;
        {$DEFINE NWE_SOCKET_TABLE_FULL}
        Const NWE_SOCKET_TABLE_FULL = $89FE;
        {$DEFINE NWE_SPOOL_DIR_ERROR}
        Const NWE_SPOOL_DIR_ERROR = $89FE;
        {$DEFINE NWE_LOGIN_DISABLED_BY_SUPER}
        Const NWE_LOGIN_DISABLED_BY_SUPER = $89FE;
        {$DEFINE NWE_TIMEOUT_FAILURE}
        Const NWE_TIMEOUT_FAILURE = $89FE;
        {$DEFINE NWE_FILE_EXT}
        Const NWE_FILE_EXT = $89FF;
        {$DEFINE NWE_FILE_NAME}
        Const NWE_FILE_NAME = $89FF;
        {$DEFINE NWE_HARD_FAILURE}
        Const NWE_HARD_FAILURE = $89FF;
        {$DEFINE NWE_FCB_CLOSE}
        Const NWE_FCB_CLOSE = $89FF;
        {$DEFINE NWE_IO_BOUND}
        Const NWE_IO_BOUND = $89FF;
        {$DEFINE NWE_BAD_SPOOL_PRINTER}
        Const NWE_BAD_SPOOL_PRINTER = $89FF;
        {$DEFINE NWE_BAD_RECORD_OFFSET}
        Const NWE_BAD_RECORD_OFFSET = $89FF;
        {$DEFINE NWE_DRIVE_INVALID_NUM}
        Const NWE_DRIVE_INVALID_NUM = $89FF;
        {$DEFINE NWE_SEM_INVALID_INIT_VAL}
        Const NWE_SEM_INVALID_INIT_VAL = $89FF;
        {$DEFINE NWE_SEM_INVALID_HANDLE}
        Const NWE_SEM_INVALID_HANDLE = $89FF;
        {$DEFINE NWE_NO_FILES_FOUND_ERROR}
        Const NWE_NO_FILES_FOUND_ERROR = $89FF;
        {$DEFINE NWE_NO_RESPONSE_FROM_SERVER}
        Const NWE_NO_RESPONSE_FROM_SERVER = $89FF;
        {$DEFINE NWE_NO_OBJ_OR_BAD_PASSWORD}
        Const NWE_NO_OBJ_OR_BAD_PASSWORD = $89FF;
        {$DEFINE NWE_PATH_NOT_LOCATABLE}
        Const NWE_PATH_NOT_LOCATABLE = $89FF;
        {$DEFINE NWE_Q_FULL_ERROR}
        Const NWE_Q_FULL_ERROR = $89FF;
        {$DEFINE NWE_REQ_NOT_OUTSTANDING}
        Const NWE_REQ_NOT_OUTSTANDING = $89FF;
        {$DEFINE NWE_SOCKET_ALREADY_OPEN}
        Const NWE_SOCKET_ALREADY_OPEN = $89FF;
        {$DEFINE NWE_LOCK_ERROR}
        Const NWE_LOCK_ERROR = $89FF;
        {$DEFINE NWE_FAILURE}
        Const NWE_FAILURE = $89FF;
    {$ENDIF}
{$ENDIF}

